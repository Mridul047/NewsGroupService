news:
  api-key: overrideThroughProgramArgs
  users:
    - authorizedUser: ${news.authorizedUser[0].username}
      password: ${news.users[0].password}
      roles: ${news.users[0].roles}
    - authorizedUser: ${news.authorizedUser[1].username}
      password: ${news.users[1].password}
      roles: ${news.users[1].roles}

jwt:
  sign-key: overrideThroughProgramArgs